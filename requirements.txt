# Core Dependencies for JUST Exposure Time Calculator (JUST ETC v1)
numpy>=1.20.0
astropy>=4.0
scipy>=1.5.0
matplotlib>=3.3.0
numba>=0.53.0

# Optional Dependencies (Only required for Redrock FITS generation & redshift fitting utilities)
# redrock
# desispec
